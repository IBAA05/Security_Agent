# ==============================================================================
# ARGUS SECURITY AGENT - FULL DEPENDENCY LIST
# ==============================================================================

# [1] CORE SECURITY AGENT LIBRARIES
# ------------------------------------------------------------------------------
pydantic==2.12.5           # High-performance data schemas (schemas.py)
PyYAML==6.0.3              # Loading scan configurations (main.py, test01_Tools.py)
requests==2.32.5           # HTTP client for DAST tools (tools/zap.py)
python-dotenv==1.2.2       # Loading .env for API keys & environment (llm/agent.py)

# [2] AGENTIC AI & REASONING (DeepSeek/GPT Integration)
# ------------------------------------------------------------------------------
openai==2.29.0             # Official LLM interface for reasoning & reflexion (llm/agent.py)

# [3] SECURITY TOOL: SEMGREP (SAST)
# ------------------------------------------------------------------------------
semgrep==1.156.0           # Python-based CLI tool for semantic code analysis

# ==============================================================================
# EXTERNAL SECURITY TOOLS SETUP (Manual Installation Required)
# ==============================================================================

# [4] GITLEAKS (Secret Scanning)
# Manual Installation Required:
# ------------------------------------------------------------------------------
# LINUX:
#   wget https://github.com/gitleaks/gitleaks/releases/download/v8.18.4/gitleaks_8.18.4_linux_x64.tar.gz
#   tar -xzf gitleaks_8.18.4_linux_x64.tar.gz
#   sudo mv gitleaks /usr/local/bin/
# MAC:
#   brew install gitleaks

# [5] TRIVY (Dependency Vulnerability Scanner)
# Manual Installation Required:
# ------------------------------------------------------------------------------
# LINUX:
#   curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin
# MAC:
#   brew install trivy

# [6] OWASP ZAP (DAST - Dynamic Analysis) Target: Staging Environments
# ------------------------------------------------------------------------------
# Download and install manually from: https://www.zaproxy.org/
# Must be running as a local daemon at http://localhost:8080 during scans.
